{
  "buildCommand": "pnpm --filter @materiaux/web build",
  "outputDirectory": "apps/web/dist",
  "installCommand": "pnpm install --frozen-lockfile",
  "rewrites": [{ "source": "/((?!assets/).*)", "destination": "/index.html" }]
}
